# Changelog

## v1.0.9 (2026-07-11) — 多调弦方案 + 自定义调弦

### ✨ 新增功能

- **多调弦方案支持**：调音器新增调弦方式选择面板，支持 7 种预设一键切换
  - 标准调弦（EADGBE）、Drop D（DADGBE）、DADGAD、Open D、Open G、半音降调、全音降调
  - 切换调弦方案时自动重置调音进度，无需手动重启
  - 记住上次使用的调弦方案，下次进入自动恢复
- **自定义调弦**：新增 `/pages/tuner/custom-tuning/` 自定义调弦页面
  - 6 根弦独立滑块调节目标音高（半音为单位）
  - 安全范围约束（基于标准调弦 ±3~5 半音），超范围显示警告，防止断弦
  - 自定义方案持久化存储，可在调弦面板中管理（选择 / 删除）
  - 滑块实时显示当前音名和频率
- **调音器引擎重构**：核心检测与状态管理逻辑拆分至独立模块
  - `utils/tuningPresets.js`：调弦预设数据 + 安全范围约束 + 工具函数
  - `utils/tunerStateMachine.js`：调音状态机（中位数滤波 + 稳定判定 + 自动切弦）
  - `utils/tunerGauge.js`：弧形仪表盘 Canvas 渲染
  - `utils/pitch.js`：自相关音高检测 + 谐波校正
- **自动/手动模式切换**：调音器新增自动切弦开关，可手动控制跳弦进度
- **调弦面板 UI**：半屏滑出面板，预设 + 自定义列表，支持长按删除自定义方案

### 🔧 优化改进

- 调音器 UI 重构：自动切弦开关、调弦面板入口、进度条交互优化
- `app.json` 注册自定义调弦页面路径
- 版本号更新至 1.0.9

### 📦 文件变更

| 文件 | 变更 |
|------|------|
| `utils/tuningPresets.js` | **新增** 7 种调弦预设 + 安全范围 + 工具函数 |
| `utils/tunerStateMachine.js` | **新增** 调音状态机 |
| `utils/tunerGauge.js` | **新增** 弧形仪表盘渲染 |
| `pages/tuner/custom-tuning/*` | **新增** 自定义调弦页面（4 个文件） |
| `pages/tuner/tuner.*` | **重构** 多方案支持 + 面板 UI |
| `app.json` | **修改** 注册自定义调弦页面 |

---

## v1.0.0 (2026) — 初始发布

### ✨ 新增功能

- **和弦查询**：12 根音 × 17 种和弦类型，覆盖大三/小三/属七/大七/减三/增三/挂留等常见和弦
- **指板图**：Canvas 2D 渲染指板按法图，支持多指法左右切换和高 DPR 适配
- **和弦试听**：Karplus-Strong 物理建模合成吉他拨弦音，多弦同时发声叠加
- **和弦说明**：每个和弦的角色、听感、调性关系、典型进行、代表歌曲等乐理知识
- **收藏夹**：支持收藏常用和弦，首页快捷跳转
- **暗色模式**：全局 CSS 自定义变量实现明暗主题一键切换
- **保存图片**：指板图支持一键保存到系统相册
- **单元测试**：Jest 测试覆盖 utils 各核心模块

### 🛠️ 技术亮点

- 指法搜索算法：数据库优先 + 回溯穷举补全 + 把位聚类去重，保证各把位区域都有代表指法

---

## v1.0.5 (2026-05-22) — 吉他调音器稳定版

### ✨ 新增功能

- **吉他调音器**：新增完整调音器页面 `/pages/tuner/tuner`
  - 支持 6 根弦实时调音（E2/A2/D3/G3/B3/E4），从 6 弦到 1 弦逐根调准
  - Canvas 绘制圆形仪表盘，深色专业风格，金色指针 + 渐变弧 + 刻度标记
  - 自相关（Autocorrelation）+ 抛物线插值音高检测引擎
  - 自动切换弦：连续调准 1.5 秒后自动跳到下一根
  - 全部调完后弹窗祝贺 + 震动反馈
  - 手动选弦：6 个圆形按钮，点击任意弦直接跳转到该弦
  - "重新开始"按钮重置全部进度
- **首页重构**：双入口卡片设计，"和弦指法查询"+"吉他调音器"并列展示

### 🐛 修复的问题

- **修复音高检测完全无响应**：录音参数 `frameSize` 设置为 5KB，确保 `onFrameRecorded` 回调被触发
- **修复指针剧烈抖动**：引入滑动 7 帧中位数滤波 + EMA 指数平滑（α=0.30），消除单帧随机跳动
- **修复环境噪声干扰**：双层噪声门限（RMS 阈值 0.008 + 自相关相关度阈值 0.25），说话/咳嗽不触发检测
- **修复低音弦检测不准**：采用"谷底→峰值"自相关策略，从最小 lag 扫描找到第一个显著相关峰，消除子谐波干扰
- **修复音准误判**：连续性判定（连续 10 帧 |cents|<5 才认为调准），避免单帧偶然通过
- **修复谐波导致的频率误检**：增加谐波校正逻辑，当检测频率偏离标准频率 >5% 时自动修正
- **修复小程序基础库兼容性**：移除 `app.json` 中不支持的 `requiredPrivateInfos`，改为动态 `wx.authorize` 请求麦克风权限

### 🔧 优化改进

- **指针缓动动画**：每帧只逼近目标角度 18%，无突变跳跃
- **偏差映射优化**：±50 音分对应指针 ±90 度，精度和可视范围平衡
- **抗噪静音提示**：无检测时 3 秒后自动显示"请拨动第 X 弦"
- **界面美化**：深色仪表盘（#1A1A34）、渐变刻度弧（橙→绿→红）、金色三角指针、外圈发光效果
- **进度指示器**：顶部 6 点进度条，已完成弦高亮绿色，当前弦高亮红色
- **状态彩色标签**：偏低(橙)、偏高(红)、接近(蓝)、音准(绿) 四种状态色
- **隐私合规**：进入调音器时先弹隐私说明，同意后再请求麦克风权限
- **调试友好**：关键步骤全部输出 `console.log`，方便真机调试定位问题

### 📦 文件变更

| 文件 | 变更 |
|------|------|
| `utils/pitch.js` | **新增** 自相关音高检测引擎 |
| `utils/pitchDetector.js` | **删除** 废弃的旧检测文件 |
| `pages/tuner/tuner.*` | **新增** 调音器页面（4 个文件） |
| `pages/chord/chord.*` | **新增** 和弦查询页面（4 个文件） |
| `pages/index/index.*` | **修改** 首页改为双入口卡片 |
| `app.json` | **修改** 注册新页面路径 |

### ⚠️ 已知问题

- 调音器在模拟器中无法测试（无麦克风），需真机调试
- 极高环境噪声（如工地、风扇直吹）可能触发错误检测，建议在相对安静的环境使用
- 部分低端安卓机型上录音帧回调延迟可能偏高，不影响使用但指针响应略慢

### 📱 兼容性

- 微信基础库 >= 2.25.0
- 需授权麦克风权限（`scope.record`）
- 仅真机有效，模拟器无麦克风可用
