# 🎸 如何吉他 (How-Guitar) — 项目详细文档

> 版本: v1.0.5 | 平台: 微信小程序 | 基础库: >= 2.25.0

---

## 一、项目概述

「如何吉他」是一个微信小程序，旨在帮助吉他学习者查询和弦指法、试听和弦发声，并提供吉他实时调音功能。

### 核心功能

| 功能 | 说明 |
|------|------|
| **和弦指法查询** | 12 个根音 × 18 种和弦类型，查看指板按法位置 |
| **指板图渲染** | Canvas 2D API 绘制高颜值指板图，支持多指法切换 |
| **和弦试听** | Karplus-Strong 算法合成真实吉他拨弦音，无需音频文件 |
| **和弦说明** | 每个和弦的角色、听感、调性关系、251 进行、代表歌曲 |
| **收藏夹** | 收藏常用和弦，首页快速跳转查看 |
| **暗色模式** | 白天/夜间主题一键切换 |
| **保存图片** | 指板图一键保存到相册 |
| **吉他调音器** | 实时音高检测，逐弦调准，支持多种调弦方式 |
| **自定义调弦** | 允许用户自定义每根弦的音高 |

### 技术栈

- **框架**: 微信小程序原生开发（WXML + WXSS + JavaScript）
- **渲染**: Canvas 2D API（指板图 + 调音器仪表盘）
- **音频合成**: Karplus-Strong 算法（吉他音色合成器）
- **音高检测**: 自相关（Autocorrelation）+ 抛物线插值
- **主题**: CSS 自定义变量实现暗色模式
- **性能**: 按需注入 + 用时注入

---

## 二、项目目录结构

```
How-Guitar/
├── .gitignore                              # Git 忽略规则
├── project.config.json                     # 微信开发者工具项目配置
├── project.private.config.json             # 私有项目配置
├── sitemap.json                            # 小程序 SEO 配置
├── CHANGELOG.md                            # 版本更新日志
├── README.md                               # 项目说明文档
├── 改进方案.md                             # (项目内部的改进计划文档)
│
├── app.js                                  # 全局入口 JS
├── app.json                                # 全局配置（页面路由、窗口设置）
├── app.wxss                                # 全局样式（浅色/暗色主题 CSS 变量）
│
├── images/
│   └── logo.png                            # 项目 Logo
│
├── pages/                                  # 页面层
│   ├── index/                              # 首页（功能入口 + 收藏夹）
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   ├── chord/                              # 和弦查询页（选择根音/类型/品数）
│   │   ├── chord.js
│   │   ├── chord.wxml
│   │   ├── chord.wxss
│   │   └── chord.json
│   ├── result/                             # 结果页（指板图 + 和弦说明 + 试听）
│   │   ├── result.js
│   │   ├── result.wxml
│   │   ├── result.wxss
│   │   └── result.json
│   └── tuner/                              # 调音器
│       ├── tuner/                          # 调音器主页面
│       │   ├── tuner.js
│       │   ├── tuner.wxml
│       │   ├── tuner.wxss
│       │   └── tuner.json
│       └── custom-tuning/                  # 自定义调弦页面
│           ├── custom-tuning.js
│           ├── custom-tuning.wxml
│           ├── custom-tuning.wxss
│           └── custom-tuning.json
│
├── components/                             # 组件层
│   ├── fretboard-viewer/                   # 指板图渲染组件
│   │   ├── fretboard-viewer.js
│   │   ├── fretboard-viewer.json
│   │   ├── fretboard-viewer.wxml
│   │   └── fretboard-viewer.wxss
│   └── placeholder/                        # 占位组件（用时注入优化）
│       ├── placeholder.js
│       ├── placeholder.json
│       ├── placeholder.wxml
│       └── placeholder.wxss
│
└── utils/                                  # 工具层
    ├── pitch.js                            # 音高检测引擎（自相关算法）
    ├── tuningPresets.js                    # 调音预设数据库
    ├── audioEngine.js                      # 音频合成引擎（Karplus-Strong）
    ├── chordDatabase.js                    # 和弦指法数据库 + 和弦类型定义
    ├── chordExplanations.js                # 和弦功能/调性说明数据
    ├── fretboard.js                        # 指法搜索算法
    └── draw.js                             # Canvas 指板图绘图引擎
```

---

## 三、文件详细说明

### 3.1 入口与全局配置

#### `app.js` — 全局入口

在微信小程序中，`App()` 函数注册了整个小程序的生命周期。

**关键代码**:
- `onLaunch()`: 小程序启动时获取系统信息（屏幕尺寸、像素比等），存入 `globalData.systemInfo`
- `globalData`: 存储全局数据，当前仅有 `systemInfo`

```javascript
App({
  onLaunch() {
    const sysInfo = wx.getSystemInfoSync();
    this.globalData.systemInfo = sysInfo;
  },
  globalData: { systemInfo: null },
});
```

---

#### `app.json` — 全局配置

定义小程序的路由、窗口样式、性能优化策略。

**页面路由（5 个页面）**:
1. `pages/index/index` — 首页
2. `pages/chord/chord` — 和弦查询页
3. `pages/result/result` — 结果展示页
4. `pages/tuner/tuner` — 调音器主页
5. `pages/tuner/custom-tuning/custom-tuning` — 自定义调弦页

**关键配置**:
- `style: "v2"`: 启用新版组件样式
- `lazyCodeLoading: "requiredComponents"`: 启用用时注入，优化首屏加载

---

#### `app.wxss` — 全局样式

使用 CSS 自定义变量（Custom Properties）实现了**完整的浅色/暗色双主题**。

**浅色主题**（默认）: 暖木色系
- 背景: `#F8F5F0`
- 卡片: `#FFFFFF`
- 强调色: `#D32F2F`（红色）
- 按钮渐变: `linear-gradient(135deg, #D32F2F, #F57C00)`

**暗色主题**（`.dark-mode` class）: 星空深蓝系
- 背景: `#141424`
- 卡片: `#22223A`
- 强调色: `#FF6B6B`（柔和红）
- 按钮渐变: `linear-gradient(135deg, #FF6B6B, #E85555)`

共定义了约 25 个主题变量，覆盖背景、卡片、文字、分割线、强调色、按钮、表格、阴影等全部 UI 元素。

---

### 3.2 页面文件

#### `pages/index/index` — 首页

**功能**: 作为应用的导航中心，提供两大功能入口和收藏夹展示。

**Data 属性**:
| 属性 | 类型 | 说明 |
|------|------|------|
| `favoriteChords` | Array | 收藏的和弦列表 |
| `darkMode` | Boolean | 是否暗色模式 |

**关键函数**:
| 函数 | 说明 |
|------|------|
| `onLoad()` | 页面加载时读取暗色模式状态和收藏夹 |
| `onShow()` | 每次显示时同步暗色模式和重新加载收藏夹 |
| `loadFavorites()` | 从 `wx.getStorageSync('favoriteChords')` 读取收藏的和弦，将其解析为显示格式（根音 + 类型名） |
| `onToggleDarkMode()` | 切换暗色模式，存储到本地，同步导航栏颜色 |
| `syncNavBarColor(isDark)` | 调用 `wx.setNavigationBarColor` 设置导航栏颜色 |
| `onGoChord()` | 跳转到和弦查询页 `/pages/chord/chord` |
| `onGoTuner()` | 跳转到调音器页 `/pages/tuner/tuner` |
| `onSelectFavorite(e)` | 点击收藏项时，将和弦信息存入 `selectedFavorite`，跳转到和弦查询页 |

**WXML 结构**:
- 顶部品牌区（🎸 图标 + 标题 + 副标题）
- 暗色模式切换按钮
- 功能入口卡片：和弦指法查询（🎵）、吉他调音器（🎛️）
- 收藏夹横向滚动列表（仅在有收藏时显示）

---

#### `pages/chord/chord` — 和弦查询页

**功能**: 用户选择根音、和弦类型、指板范围，生成并跳转到结果页。

**Data 属性**:
| 属性 | 说明 |
|------|------|
| `rootNotes` | 12 个半音根音列表 `['C','C#','D',...,'B']` |
| `selectedRoot` | 当前选中的根音 |
| `selectedRootIndex` | 当前根音索引 |
| `chordTypes` | 18 种和弦类型（大三、小三、属七...） |
| `selectedTypeIndex` | 当前类型索引 |
| `maxFret` | 指板范围（5 或 12 品） |
| `previewNotes` | 和弦组成音字符串 |
| `previewNotesArray` | 和弦组成音数组 |
| `isLoading` | 生成按钮加载状态 |

**关键函数**:
| 函数 | 说明 |
|------|------|
| `onLoad()` | 读取暗色模式；检查 `selectedFavorite` 是否有从首页传过来的和弦参数，若有则自动选中该和弦 |
| `selectRoot(e)` | 选中根音，触发 `updatePreview()` |
| `selectType(e)` | 选中和弦类型，触发 `updatePreview()` |
| `onSetMaxFret(e)` | 设置指板范围（5 或 12 品） |
| `updatePreview()` | 调用 `getChordNotes(root, type)` 获取和弦组成音并显示 |
| `onGenerate()` | 调用 `getChordFingerings(root, type, maxFret)` 搜索指法，将结果存入 `currentFingeringResult`，跳转到结果页 |

**交互流程**: 选择根音 → 选择类型 → 预览组成音 → 选择品数 → 点击「查看指法」→ 跳转到结果页

---

#### `pages/result/result` — 结果展示页

**功能**: 显示和弦指板图、和弦说明、弦位详情表，支持指法切换、和弦试听、收藏、保存图片。

**Data 属性**:
| 属性 | 说明 |
|------|------|
| `chordName` | 和弦名称（如 "C"、"Am"） |
| `chordNotes` | 组成音字符串 |
| `fingerings` | 所有指法列表 |
| `totalCount` | 指法总数 |
| `currentIndex` | 当前指法索引 |
| `currentFingering` | 当前指法对象 |
| `stringDetails` | 6 根弦的详情（弦名、音名、品数、状态） |
| `chordInfo` | 和弦说明对象（角色、听感、功能等） |
| `isFavorited` | 是否已收藏 |
| `isPlaying` | 是否正在播放音频 |
| `canvasWidth/Height` | Canvas 尺寸 |

**关键函数**:
| 函数 | 说明 |
|------|------|
| `onLoad()` | 从 `currentFingeringResult` 读取指法数据；计算最佳 Canvas 尺寸；获取和弦说明；检查收藏状态；调用 `showFingering(0)` 显示第一个指法 |
| `onUnload()` | 页面卸载时调用 `audioEngine.stopAll()` 停止播放 |
| `guessMaxFret(fingerings)` | 根据所有指法的最高品位确定 Canvas 需要显示的最大品数 |
| `showFingering(index)` | 显示指定索引的指法：渲染弦位详情表（弦名、音名、品数、状态），并通知 fretboard-viewer 组件重绘 |
| `prevFingering() / nextFingering()` | 切换上一个/下一个指法 |
| `onPlayChord()` | 播放/停止和弦：调用 `audioEngine.playChord()` 播放当前指法 |
| `onToggleFavorite()` | 切换收藏状态，存储到 `favoriteChords` |
| `onSaveImage()` | 调用 fretboard-viewer 组件的 `saveToAlbum()` 将指板图保存到相册 |

**WXML 结构**:
- 结果头部（和弦名 + 组成音 + 收藏按钮）
- `<fretboard-viewer>` 自定义组件（指板图）
- 操作栏（指法切换 + 播放按钮）
- 和弦说明卡片（角色、听感、常出现在、功能、典型进行、代表歌曲）
- 弦位详情表格（6 弦 × 4 列）
- 保存图片按钮

---

#### `pages/tuner/tuner` — 调音器主页

**功能**: 实时吉他调音核心页面。使用麦克风采集音频，自相关算法检测音高，Canvas 绘制圆形仪表盘显示音准偏差。

**Data 属性**:
| 属性 | 说明 |
|------|------|
| `showPrivacy` | 是否显示隐私说明弹窗 |
| `showPermTip` | 是否显示权限提示 |
| `showComplete` | 是否显示调音完成弹窗 |
| `tuningOrder` | 当前调弦的 6 根弦数据 |
| `currentIndex` | 当前正在调的弦索引 |
| `currentInfo` | 当前弦的信息（弦号、音名、目标频率） |
| `currentTuningId` | 当前调弦方案 ID |
| `currentTuningName` | 当前调弦名称 |
| `tuningList` | 所有可用的调弦方案列表 |
| `showTuningPanel` | 是否显示调弦方案选择面板 |
| `statusClass` | 状态样式类（waiting/flat/sharp/near/tuned） |
| `statusText` | 状态提示文字 |
| `freqDisplay` | 检测到的频率显示 |
| `noteDisplay` | 检测到的音名显示 |
| `centsDisplay` | 音分偏差显示 |

**实例变量**:
| 变量 | 说明 |
|------|------|
| `recMgr` | 微信录音管理器实例 |
| `isRunning` | 录音是否运行中 |
| `canvasCtx` | Canvas 2D 上下文 |
| `frameBuffer` | 帧缓冲区（合并多帧用于检测） |
| `freqHistory` | 频率历史记录（用于中位数滤波） |
| `smoothAngle` | 平滑后的指针角度 |
| `stableCount` | 音准连续稳定计数（连续 10 帧才算调准） |
| `drawAngle` | 当前绘制的指针角度 |
| `drawTimer` | Canvas 重绘定时器 |
| `pluckTimer` | 提示用户拨弦的定时器 |

**关键函数**:
| 函数 | 说明 |
|------|------|
| `onLoad()` | 初始化 Canvas、加载调弦列表、读取上次使用的调弦方案 |
| `onUnload()` | 停止录音、清除所有定时器 |
| `initCanvas()` | 创建 Canvas 2D 上下文，启动 50ms 间隔的重绘定时器 `drawGauge` |
| `drawGauge()` | 核心绘图函数：绘制深色仪表盘背景 → 渐变刻度弧（橙→黄→绿→橙→红）→ 刻度线 → 三角指针 → 中心圆 → 弦信息文字 |
| `startRec()` | 初始化录音管理器，设置 `frameSize: 5`（5KB 帧），注册 `onFrameRecorded` 回调；将 Int16 PCM 数据转为 Float32，合并多帧后调用 `pitch.detectPitch()` |
| `stopRec()` | 停止录音，清空缓冲区 |
| `onPitch(result)` | 音高检测结果处理：中位数滤波 → 计算音分偏差 → EMA 平滑角度 → 更新状态（偏低/偏高/接近/音准）；连续 10 帧音准稳定则触发 `onStringDone()` |
| `onStringDone()` | 当前弦调准完成：震动反馈 → 自动切换到下一弦（若全部完成则弹出完成弹窗） |
| `onTapString(e)` | 手动点击某弦直接跳转 |
| `onRestart()` | 重置所有状态，从第 1 弦重新开始 |
| `switchTuning(tuningId)` | 切换调弦方案，重置调音状态 |
| `onToggleTuningPanel()` | 显示/隐藏调弦方案选择面板 |
| `onSelectTuning(e)` | 选择新的调弦方案并切换 |

**WXML 结构**:
- 隐私协议遮罩弹窗
- 进度条（6 个圆点，已完成=绿色，当前=红色）
- 弦信息 + 目标频率
- 状态标签（颜色随状态变化）
- Canvas 仪表盘（圆形刻度盘 + 指针）
- 频率/音名/音分显示
- 手动选弦按钮（6 个圆形按钮）
- 重新开始按钮
- 调弦方案切换按钮（右下角齿轮图标）
- 完成弹窗
- 调弦方案选择面板（底部弹出，支持选择预设 + 自定义 + 删除自定义）

---

#### `pages/tuner/custom-tuning/custom-tuning` — 自定义调弦页

**功能**: 允许用户自定义 6 根弦的音高（通过滑杆选择半音值），保存为自定义调弦方案。

**Data 属性**:
| 属性 | 说明 |
|------|------|
| `tuningName` | 调弦名称 |
| `strings` | 6 根弦数据（含 min/max 半音范围、当前半音值、音名、频率、警告状态） |
| `saveDisabled` | 保存按钮是否禁用 |

**关键函数**:
| 函数 | 说明 |
|------|------|
| `onLoad()` | 初始化 6 根弦的默认音高为 `STRING_SAFE_RANGE` 中的标准值 |
| `onNameInput(e)` | 输入调弦名称时触发，检查保存条件 |
| `onSliderChanging(e)` | 滑杆拖动时实时更新半音值、音名、频率，并检查是否超出安全范围 |
| `onSave()` | 保存自定义调弦到 `customTunings` 存储，自动跳回调音器页 |
| `onCancel()` | 返回上一页 |
| `checkSaveEnabled()` | 检查保存条件：名称非空 + 所有弦在安全范围内 |

---

### 3.3 组件

#### `components/fretboard-viewer/` — 指板图组件

**功能**: 封装了指板图的 Canvas 渲染和保存功能，可通过属性传递数据。

**Properties**:
| 属性 | 类型 | 说明 |
|------|------|------|
| `canvasWidth` | Number | Canvas 宽度（px） |
| `canvasHeight` | Number | Canvas 高度（px） |
| `stringDetails` | Array | 6 根弦的详情（用于左侧标签渲染） |
| `fingerings` | Array | 所有指法数据 |
| `maxFret` | Number | 最大品数 |
| `currentIndex` | Number | 当前选中的指法索引 |

**方法**:
| 方法 | 说明 |
|------|------|
| `drawCanvas()` | 获取 Canvas 节点，调用 `draw.drawFretboard()` 渲染指板图 |
| `saveToAlbum()` | 将 Canvas 转为临时图片路径，调用 `wx.saveImageToPhotosAlbum()` 保存到相册 |

**Observers**:
- 监听 `currentIndex` 变化，自动触发 `drawCanvas()`

**性能优化**: 使用 `require('../../utils/draw')` 延时加载绘图模块（首次绘图时才 require）。

**WXML 结构**:
- 左侧固定标签列（弦名 + 空弦/制音标记）
- 右侧可横向滑动的 Canvas 指板图

---

#### `components/placeholder/` — 占位组件

**功能**: 配合微信小程序的 `componentPlaceholder`（用时注入）机制，在结果页未渲染前显示加载占位。

**Properties**: 仅接收 `canvasHeight` 用于适配高度。

**WXML**: 显示 🎸 图标 + "加载指板图中..." 文字。

---

### 3.4 工具模块

#### `utils/pitch.js` — 音高检测引擎

**功能**: 核心音高检测模块，使用自相关（Autocorrelation）算法 + 抛物线插值实现高精度频率检测。

**导出函数**:
| 函数 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `freqToNote(freq)` | 频率 | `{name, cents, semitone, freq}` | 将频率转换为音名（如 440 → "A4"） |
| `calcCents(detectedFreq, targetFreq)` | 检测频率, 目标频率 | 音分数 | 计算两个频率之间的音分偏差 |
| `centsToAngle(cents)` | 音分数 | 角度（-90 ~ 90） | 将音分偏差映射到仪表盘指针角度 |
| `smoothEMA(value, prev, alpha)` | 当前值, 上一帧值, 平滑系数 | 平滑后的值 | 指数移动平均平滑 |
| `medianOfBuffer(buf)` | 数值数组 | 中位数 | 中位数滤波器，去除异常值 |
| `detectPitch(samples, sampleRate, targetTunings)` | PCM 样本, 采样率, 目标频率列表 | `{frequency, confidence, rms}` 或 `null` | 核心音高检测函数 |

**`detectPitch()` 算法流程**:
1. **RMS 噪声门限**: 计算均方根值，低于 `0.008` 直接返回 null
2. **直流偏置移除**: 减去均值
3. **自相关计算**: 在 `minLag~maxLag` 范围内计算归一化自相关值
4. **峰值检测**: 找出所有 ≥ 全局最大值 85% 的相关峰
5. **基频选择**: 取第一个（最小 lag）显著峰作为基频
6. **抛物线插值**: 对峰值周围 3 点进行抛物线插值，获得亚样本精度
7. **谐波校正**: 将检测频率与目标调弦频率对比，若偏差 > 5% 则尝试除以倍频（2~5 倍），校正谐波误检
8. **频率范围检查**: 65Hz ~ 1100Hz（覆盖吉他全部音域）

**常量**:
| 常量 | 值 | 说明 |
|------|-----|------|
| `RMS_THRESHOLD_ON` | 0.008 | 噪声门限开启阈值 |
| `RMS_THRESHOLD_OFF` | 0.004 | 噪声门限关闭阈值 |
| `PEAK_CORR_THRESHOLD` | 0.25 | 自相关相关度阈值 |
| `FREQ_MIN` | 65 | 最小可检测频率（≈ C2） |
| `FREQ_MAX` | 1100 | 最大可检测频率（≈ C6） |

---

#### `utils/tuningPresets.js` — 调音预设数据库

**功能**: 存储和管理各种吉他调弦方案，提供半音与频率/音名之间的转换函数。

**预设调弦方案（7 种）**:
| ID | 名称 | 调弦 |
|----|------|------|
| `standard` | 标准调弦 | E2 A2 D3 G3 B3 E4 |
| `drop-d` | Drop D | D2 A2 D3 G3 B3 E4 |
| `dadgad` | DADGAD | D2 A2 D3 G3 A3 D4 |
| `open-d` | Open D | D2 A2 D3 F#3 A3 D4 |
| `open-g` | Open G | D2 G2 D3 G3 B3 D4 |
| `half-step-down` | 半音降调 | Eb2 Ab2 Db3 Gb3 Bb3 Eb4 |
| `full-step-down` | 全音降调 | D2 G2 C3 F3 A3 D4 |

**弦的安全范围（`STRING_SAFE_RANGE`）**:
| 弦号 | 标准半音 | 最小半音 | 最大半音 |
|------|---------|---------|---------|
| 6 弦 | 40 (E2) | 35 | 43 |
| 5 弦 | 45 (A2) | 40 | 48 |
| 4 弦 | 50 (D3) | 45 | 51 |
| 3 弦 | 55 (G3) | 52 | 57 |
| 2 弦 | 59 (B3) | 56 | 61 |
| 1 弦 | 64 (E4) | 61 | 66 |

**导出函数**:
| 函数 | 说明 |
|------|------|
| `semitoneToFreq(semitone)` | 半音值 → 频率（A4=440Hz，12 平均律） |
| `semitoneToNote(semitone)` | 半音值 → 音名（如 69 → "A4"） |
| `getTuningById(id)` | 根据 ID 获取调弦方案 |
| `getAllPresets()` | 获取所有预设调弦方案 |
| `isNoteInSafeRange(stringIndex, semitone)` | 检查音高是否在安全范围内 |
| `getValidSemitonesForString(stringIndex)` | 获取某弦允许的所有半音值 |

---

#### `utils/audioEngine.js` — 音频合成引擎

**功能**: 使用 Karplus-Strong 算法合成吉他拨弦音效，无需任何音频文件。

**核心算法 — `generatePluckBufferWarm()`**:
1. **初始化**: 前 N 个样本填充白噪声（模拟拨弦扰动），幅度 ±0.45
2. **KS 递推**: `y[n] = (y[n-N] + y[n-N+1]) * 0.496`（四点平均 + 衰减）
3. **指数包络**: `e^(-3.8 * t)` 使声音平滑衰减
4. **箱体滤波**: 一阶低通滤波，模拟木质共鸣吸收高频

**关键函数**:
| 函数 | 说明 |
|------|------|
| `noteToFrequency(note)` | 音名 → 频率（如 "E2" → 82.41Hz） |
| `getSoundingNotes(strings)` | 解析指法数据，获取所有发声的音符列表 |
| `generatePluckBuffer(ctx, freq, duration)` | 生成基本 KS 拨弦 AudioBuffer |
| `generatePluckBufferWarm(ctx, freq, duration)` | 生成带箱体滤波的温暖版拨弦 AudioBuffer |
| `playChord(strings, onEnd)` | 播放和弦：为每个音符生成独立的 KS buffer，通过独立的 GainNode 控制音量（低音弦轻、高音弦重），同时启动播放 |
| `stopAll()` | 停止所有播放并关闭 AudioContext |

**音量控制**: 各弦音量从 0.35（6 弦）到 0.60（1 弦），模拟真实吉他各弦的音量差异。

---

#### `utils/chordDatabase.js` — 和弦数据库

**功能**: 包含两部分内容：1）手写和弦指法数据库；2）和弦类型音程定义及音名处理工具。

**手写指法数据库（`CHORD_DATABASE`）**:
覆盖 12 个根音 × 多种类型的常用指法（前 5 品），包括：

| 根音 | 支持的和弦类型 |
|------|--------------|
| C | major, m, maj7, 7, add9 |
| D | major, m, m7, 7, sus2, sus4 |
| E | major, m, m7, 7 |
| F | major, maj7 |
| G | major, 7, G/B |
| A | major, m, m7, 7, sus2, sus4 |
| B | dim, m, 7 |
| Bb | major |
| F# | m |
| Eb/C# | m |
| G#/Ab | m |
| A#/Bb | m |

每个指法包含 `name`（描述性名称）和 `strings` 数组（6 根弦从 6 弦到 1 弦，每根弦有 `fret`、`note`、`label`）。

**和弦音程定义（`CHORD_TYPES`）**:
定义 20 种和弦类型的半音音程：
| 类型 | 音程 | 类型 | 音程 |
|------|------|------|------|
| major | 0,4,7 | m | 0,3,7 |
| 7 | 0,4,7,10 | maj7 | 0,4,7,11 |
| m7 | 0,3,7,10 | dim | 0,3,6 |
| aug | 0,4,8 | sus2 | 0,2,7 |
| sus4 | 0,5,7 | m7b5 | 0,3,6,10 |
| 6 | 0,4,7,9 | m6 | 0,3,7,9 |
| add9 | 0,4,7,2 | 9 | 0,4,7,10,2 |
| m9 | 0,3,7,10,2 | maj9 | 0,4,7,11,2 |
| 7sus4 | 0,5,7,10 | 11 | 0,4,7,10,2,5 |
| 13 | 0,4,7,10,2,5,9 | | |

**导出函数**:
| 函数 | 说明 |
|------|------|
| `noteToSemitone(note)` | 音名字符串 → 半音索引（0~11） |
| `semitoneToNote(semi)` | 半音索引 → 升号音名 |
| `getSmartNoteName(root, type, semi)` | 智能选择升号/降号音名（根据根音和和弦类型） |
| `getChordNotes(root, type)` | 获取和弦组成音（含根音、类型、音名列表、半音值列表） |
| `getChordFromDatabase(root, type)` | 从数据库查询预定义指法 |
| `getAllRootNotes()` | 获取所有根音列表 |
| `getAllChordTypes()` | 获取所有和弦类型列表 |

---

#### `utils/fretboard.js` — 指法搜索算法

**功能**: 核心算法模块，通过回溯搜索生成所有合理的和弦指法。

**核心数据结构 — `OPEN_STRINGS`**:
标准调弦 6 根弦的空弦信息（索引、音名、半音值）。

**关键函数**:
| 函数 | 说明 |
|------|------|
| `getNoteAtFret(stringIndex, fret)` | 获取某弦某品的音名和半音值 |
| `getChordPositionsOnString(stringIndex, chordNoteValues, maxFret)` | 获取某弦上所有可能按出和弦音的位置（含空弦） |
| `findAllFingerings(chordNoteValues, maxFret)` | 核心搜索函数：回溯算法，遍历所有组合可能性 |
| `calcFretSpan(strings)` | 计算指法的品数跨度 |
| `isFingeringReasonable(strings)` | 检查指法是否合理（至少 3 根弦发声、无中间空洞、制音弦 ≤ 2 根） |
| `scoreAlgorithmFingering(fingering, root)` | 算法指法评分（开放弦加分、低品位加分、跨度小加分、弦数多加分、根音在低音弦加分） |
| `getChordFingerings(root, type, maxFret)` | **对外主接口**：先查数据库 → 算法生成 → 综合排序 → 最多返回 20 个指法 |

**`findAllFingerings()` 算法约束**:
1. 每根弦可选：制音(x)、空弦（仅当空弦音是和弦音）、按某品（必须是和弦音）
2. 所有和弦音至少出现一次
3. 至少弹 3 根弦
4. 通过去重指纹（`"string-fret"` 组合）避免重复
5. 剪枝：剩余弦数不够覆盖剩余音符时提前返回

**`getChordFingerings()` 排序规则**:
1. 数据库手写指法优先于算法生成指法
2. 数据库指法按原始顺序排列
3. 算法指法按综合评分降序排列（开放弦 + 低品位 + 小跨度 + 多弦数 + 根音位置）

---

#### `utils/draw.js` — Canvas 绘图引擎

**功能**: 在 Canvas 上绘制吉他指板图。

**绘图流程 — `drawFretboard(ctx, options)`**:
1. **清空画布**，填充指板背景色 `#FFF8E1`
2. **绘制品丝**：竖线（0 ~ maxFret），灰色 `#999`，线宽 1.5px
3. **绘制琴弦**：横线（6 弦到 1 弦），不同粗细（1.5~4px），颜色从灰色到深色
4. **绘制琴枕**：左侧 4px 宽的深色竖条
5. **绘制品格标记圆点**：3、5、7、9 品单点，12 品双点
6. **绘制品数文字**：底部标注 1~maxFret
7. **绘制指法圆点**：红色圆点 + 白色高光 + 白色标签文字

**尺寸计算**:
- 品间距 = 指板宽度 / (maxFret + 1)
- 弦间距 = 指板高度 / 5
- 圆点半径根据 maxFret 自适应（5~7px）
- 字体大小根据 maxFret 自适应（9~11px）

---

#### `utils/chordExplanations.js` — 和弦说明数据

**功能**: 为每个常用和弦提供详细的教学说明，帮助学习者理解其在音乐中的角色。

**数据结构**: `CHORD_INFO` 对象，key 为和弦名称（如 "C"、"Am"、"G7"），value 包含：

| 字段 | 说明 |
|------|------|
| `role` | 在调性中的角色（主和弦/属和弦/下属和弦/小调主和弦等） |
| `feeling` | 听感描述 |
| `keys` | 出现在哪些调中（如 "C大调（Ⅰ级）、F大调（Ⅴ级）"） |
| `function` | 功能详细说明 |
| `progression` | 典型和弦进行 |
| `songExample` | 代表歌曲例子 |

**通用类型说明**: 对于没有特定根音的和弦类型（如减三、增三、挂留等），使用 `TYPE_` 前缀提供通用说明。

**`getChordExplanation(root, type)` 函数**:
1. 优先查找精确匹配（如 "Cmaj7"）
2. 没有精确匹配则查类型通用说明（如 "TYPE_maj7"）
3. 仍未匹配则返回大调或小调三元组的通用模板

---

## 四、数据流与页面交互

### 4.1 和弦查询主流程

```
首页 (index)
  │ onGoChord()
  ▼
和弦查询页 (chord)
  ├─ selectRoot() + selectType() → updatePreview()
  │    调用 getChordNotes(root, type) [chordDatabase.js]
  │    显示组成音
  ├─ onSetMaxFret() → 设置品数
  └─ onGenerate() → 调用 getChordFingerings(root, type, maxFret) [fretboard.js]
        ├─ 先查 CHORD_DATABASE [chordDatabase.js]
        ├─ 再用 findAllFingerings() 回溯算法生成
        ├─ scoreAlgorithmFingering() 排序
        └─ 存入 Storage → 跳转
              │
              ▼
结果页 (result)
  ├─ showFingering() → fretboard-viewer 组件
  │     └─ drawCanvas() → draw.drawFretboard() [draw.js]
  ├─ onPlayChord() → audioEngine.playChord() [audioEngine.js]
  │     └─ generatePluckBufferWarm() (Karplus-Strong)
  ├─ 和弦说明 → chordExplanations.getChordExplanation() [chordExplanations.js]
  ├─ 收藏 → wx.setStorageSync('favoriteChords')
  └─ 保存图片 → fretboard-viewer.saveToAlbum() → wx.canvasToTempFilePath()
```

### 4.2 调音器主流程

```
调音器页 (tuner)
  ├─ 隐私说明弹窗 → 同意 → wx.authorize('scope.record')
  ├─ startRec()
  │     ├─ wx.getRecorderManager()
  │     ├─ onFrameRecorded → Int16 → Float32 → 合并多帧
  │     └─ pitch.detectPitch() [pitch.js]
  │           ├─ RMS 噪声门限
  │           ├─ 自相关计算
  │           ├─ 峰值检测 + 抛物线插值
  │           └─ 谐波校正
  ├─ onPitch(result)
  │     ├─ 中位数滤波 (MEDIAN_WINDOW=7)
  │     ├─ calcCents() → centsToAngle()
  │     ├─ EMA 平滑 (α=0.30)
  │     ├─ 更新状态显示 (flat/sharp/near/tuned)
  │     └─ 连续 10 帧音准 → onStringDone()
  ├─ onStringDone()
  │     ├─ wx.vibrateShort() 震动反馈
  │     └─ 自动切换到下一弦 / 完成弹窗
  ├─ drawGauge() [50ms 定时器]
  │     └─ Canvas 绘制仪表盘 + 指针缓动动画
  └─ switchTuning() → 切换调弦方案 [tuningPresets.js]
```

---

## 五、存储键值说明

| Storage Key | 类型 | 说明 |
|-------------|------|------|
| `darkMode` | Boolean | 暗色模式开关 |
| `favoriteChords` | String[] | 收藏的和弦键列表（如 `["C_major", "Am_m"]`） |
| `selectedFavorite` | Object | 从首页收藏夹跳转时传递的和弦参数 `{root, type}` |
| `currentFingeringResult` | Object | 和弦查询结果，传给结果页 |
| `customTunings` | Object[] | 用户自定义调弦方案列表 |
| `lastTuningId` | String | 上次使用的调弦方案 ID |

---

## 六、性能优化策略

1. **用时注入** (`componentPlaceholder`): 结果页的 `fretboard-viewer` 组件在页面初次渲染时用 `placeholder` 代替，减少首屏渲染时间
2. **按需注入** (`lazyCodeLoading`): 启用微信小程序的按需注入特性
3. **延时加载**: `fretboard-viewer.js` 中的 `drawModule` 在首次绘图时才 `require`
4. **中位数滤波**: 调音器中的频率历史使用 7 帧中位数滤波，消除单帧异常值
5. **EMA 平滑**: 仪表盘指针角度和频率检测结果都使用指数移动平均平滑
6. **Canvas 缓存**: 使用定时器以 50ms 间隔重绘仪表盘，避免频繁触发
7. **指法去重**: `findAllFingerings` 使用指纹字符串避免重复生成相同指法

---

## 七、关键依赖关系图

```
app.js / app.json / app.wxss
      │
      ├── pages/index/index.js
      │     └── utils/chordDatabase.js
      │     └── utils/fretboard.js
      │
      ├── pages/chord/chord.js
      │     └── utils/chordDatabase.js (getChordNotes)
      │     └── utils/fretboard.js (getChordFingerings)
      │
      ├── pages/result/result.js
      │     ├── utils/chordExplanations.js (getChordExplanation)
      │     ├── utils/audioEngine.js (playChord, stopAll)
      │     └── components/fretboard-viewer/
      │           └── utils/draw.js (drawFretboard)
      │
      ├── pages/tuner/tuner.js
      │     ├── utils/pitch.js (detectPitch, calcCents, centsToAngle, ...)
      │     └── utils/tuningPresets.js (getTuningById, getAllPresets)
      │
      └── pages/tuner/custom-tuning/custom-tuning.js
            └── utils/tuningPresets.js (semitoneToNote, semitoneToFreq, ...)
```

---

## 八、版本历史

| 版本 | 日期 | 主要内容 |
|------|------|---------|
| v1.0.5 | 2026-05-22 | 新增吉他调音器、首页重构、和弦查询独立页面 |
| v1.0.0 | - | 首次发布（和弦查询 + 指板图 + 和弦试听） |

---

*本文档由工具自动生成，基于 v1.0.5 版本的代码分析。*